# Server-only. Never expose this value to browser code.
OPENAI_API_KEY=

# Phase 1 uses the deterministic prepared fixture. Enable only after the
# schema-constrained GPT-5.6 adapter is implemented and an API key is present.
NOTZERO_ENABLE_LIVE_ANALYSIS=false
NOTZERO_MODEL=gpt-5.6
NOTZERO_ANALYSIS_VERSION=phase-1
